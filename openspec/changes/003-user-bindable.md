# 变更提案：用户管理与邀请绑定

## 概述

| 属性 | 值 |
|------|-----|
| 提案编号 | 003 |
| 状态 | ✅ 已完成 |
| 优先级 | 高 |
| 预计工时 | 5小时 |

## 目标

实现用户管理功能（添加老师/学生）和邀请绑定功能（发送邀请、接受绑定），支持两种入驻方式：被邀请绑定和自主注册。

## 范围

### 页面

```
miniprogram/pages/
├── register/           # 自主注册页
│   ├── register.js
│   ├── register.json
│   ├── register.wxml
│   └── register.wxss
├── users/              # 用户管理
│   ├── list/           # 用户列表页
│   │   ├── list.js
│   │   ├── list.json
│   │   ├── list.wxml
│   │   └── list.wxss
│   ├── add/            # 添加用户页
│   │   ├── add.js
│   │   ├── add.json
│   │   ├── add.wxml
│   │   └── add.wxss
│   └── detail/         # 用户详情页
│       ├── detail.js
│       ├── detail.json
│       ├── detail.wxml
│       └── detail.wxss
└── bind/               # 邀请绑定页（002已创建）
```

### 云函数

```
cloud/functions/
└── user/
    ├── index.js        # 入口（已有）
    ├── register.js     # 自主注册
    ├── add.js          # 添加用户记录
    ├── list.js         # 用户列表
    ├── detail.js       # 用户详情
    ├── invite.js       # 发送邀请
    ├── acceptInvite.js # 接受邀请（独立用户）
    └── package.json
```

## 实现细节

### 1. 自主注册页面

#### 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        自主注册流程                              │
└─────────────────────────────────────────────────────────────────┘

  ┌──────────┐     ┌──────────────┐     ┌──────────────┐
  │  登录页  │────▶│  点击注册    │────▶│  选择角色    │
  │          │     │              │     │  老师/学生   │
  └──────────┘     └──────────────┘     └──────────────┘
                                               │
                                               ▼
  ┌──────────┐     ┌──────────────┐     ┌──────────────┐
  │  注册    │◀────│  填写信息    │◀────│  获取手机号   │
  │  成功    │     │  姓名/密码   │     │  微信授权    │
  └──────────┘     └──────────────┘     └──────────────┘
       │
       ▼
  ┌──────────────────────────────────────────────────┐
  │  成为独立用户（bindStatus = 'registered'）        │
  │  后续可接受邀请，与校长/老师建立关系               │
  └──────────────────────────────────────────────────┘
```

#### UI 设计

```
┌────────────────────────────────────┐
│        ◀  注册                     │
├────────────────────────────────────┤
│                                    │
│  选择您的角色：                     │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  👨‍🏫                          │ │
│  │  我是老师                     │ │
│  │  可创建课程、邀请学生          │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  👨‍🎓                          │ │
│  │  我是学生                     │ │
│  │  可查看课程表、选课            │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  📱 获取微信手机号            │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  👤 请输入姓名                │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  🔒 设置密码                  │ │
│  └──────────────────────────────┘ │
│  ┌──────────────────────────────┐ │
│  │  🔒 确认密码                  │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │          注 册                │ │
│  └──────────────────────────────┘ │
│                                    │
│       已有账号？去登录 →           │
│                                    │
└────────────────────────────────────┘
```

### 2. 用户列表页面（我的老师 / 我的学生）

#### 入口说明
首页拆分为两个独立入口：
- **我的老师**：仅校长可见，通过 `/pages/users/list/list?type=teacher` 访问
- **我的学生**：校长和老师可见，通过 `/pages/users/list/list?type=student` 访问

#### 权限说明
| 角色 | 我的老师 | 我的学生 |
|-----|---------|---------|
| 校长 | ✅ 查看全部老师 | ✅ 查看全部学生 |
| 老师 | ❌ 不可见 | ✅ 仅自己创建的学生 |
| 学生 | ❌ 不可见 | ❌ 不可见 |

#### UI 设计（无 Tab 切换，单一列表）

```
┌────────────────────────────────────┐
│      ◀  我的老师             ＋    │ ← 导航标题根据类型变化
├────────────────────────────────────┤
│                                    │
│  ┌────────────────────────────┐   │
│  │  🔍 搜索姓名...            │   │
│  └────────────────────────────┘   │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ 👤 张老师                    │ │
│  │    数学  │  ✅ 已绑定        │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ 👤 李老师                    │ │
│  │    英语  │  ⏳ 待绑定        │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ 👤 王老师                    │ │
│  │    物理  │  📝 未发邀请      │ │
│  └──────────────────────────────┘ │
│                                    │
└────────────────────────────────────┘
```

#### 状态标签
| 状态 | 显示 | bindStatus |
|------|------|------------|
| 📝 未发邀请 | 灰色 | unbound |
| ⏳ 待绑定 | 黄色 | pending |
| ✅ 已绑定 | 绿色 | bound |
| 🔗 独立用户 | 蓝色 | registered |

### 3. 添加用户页面

#### UI 设计 - 添加老师

```
┌────────────────────────────────────┐
│        ◀  添加老师                 │
├────────────────────────────────────┤
│                                    │
│  ┌──────────────────────────────┐ │
│  │  👤 老师姓名 *               │ │
│  │     请输入老师姓名            │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  📚 教授科目                  │ │
│  │     请输入教授科目            │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  📝 备注                      │ │
│  │     可选填写备注信息          │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │          保 存                │ │
│  └──────────────────────────────┘ │
│                                    │
│  提示：保存后可发送邀请给真实用户   │
│                                    │
└────────────────────────────────────┘
```

### 4. 用户详情页（发送邀请）

#### UI 设计

```
┌────────────────────────────────────┐
│        ◀  老师详情                 │
├────────────────────────────────────┤
│                                    │
│         ┌────────────┐            │
│         │    👤      │            │
│         │   张老师    │            │
│         └────────────┘            │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  角色：老师                   │ │
│  │  科目：数学                   │ │
│  │  状态：📝 未发邀请            │ │
│  │  创建时间：2024-01-15         │ │
│  └──────────────────────────────┘ │
│                                    │
│  ═══════════════════════════════  │
│                                    │
│  ┌──────────────────────────────┐ │
│  │      📤 邀请绑定              │ │ ← 生成分享卡片
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │      ✏️ 编辑信息              │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │      🗑️ 删除                  │ │
│  └──────────────────────────────┘ │
│                                    │
└────────────────────────────────────┘
```

### 5. 邀请分享卡片

```
┌────────────────────────────────────┐
│                                    │
│         📅 排课系统                │
│                                    │
│    张校长 邀请您成为                │
│    「数学老师」                     │
│                                    │
│    点击加入 →                      │
│                                    │
└────────────────────────────────────┘
```

### 6. 云函数设计

#### register（自主注册）
```javascript
// 入参
{
  role: "teacher|student",
  phone: "手机号",
  password: "密码",
  name: "姓名",
  subject: "科目（老师）",
  className: "班级（学生）"
}

// 处理逻辑
// 1. 检查手机号是否已注册
// 2. 创建用户记录（bindStatus = 'registered'）
// 3. 返回用户信息和 token

// 返回
{
  success: true,
  data: {
    userId: "用户ID",
    role: "角色",
    name: "姓名",
    token: "登录凭证"
  }
}
```

#### add（添加用户记录）
```javascript
// 入参
{
  role: "teacher|student",
  name: "姓名",
  subject: "科目（老师）",
  className: "班级（学生）",
  remark: "备注"
}

// 处理逻辑
// 1. 验证当前用户权限（校长可添加老师/学生，老师只能添加学生）
// 2. 创建用户记录（bindStatus = 'unbound'）
// 3. 创建关系记录

// 返回
{
  success: true,
  data: {
    userId: "新用户ID"
  }
}
```

#### invite（发送邀请）
```javascript
// 入参
{
  targetUserId: "目标用户ID"
}

// 处理逻辑
// 1. 验证目标用户状态（必须是 unbound 或 pending）
// 2. 生成邀请码（6位）
// 3. 创建邀请记录
// 4. 更新用户 bindStatus 为 'pending'

// 返回
{
  success: true,
  data: {
    inviteId: "邀请ID",
    code: "ABC123",
    expireTime: "过期时间"
  }
}
```

#### acceptInvite（独立用户接受邀请）
```javascript
// 入参
{
  code: "邀请码"
}

// 处理逻辑（针对已注册的独立用户）
// 1. 验证邀请码有效性
// 2. 检查当前用户是否为独立用户（bindStatus = 'registered'）
// 3. 创建关系记录
// 4. 更新邀请状态为 'accepted'

// 返回
{
  success: true,
  data: {
    relationId: "关系ID",
    inviter: { name, role }
  }
}
```

### 7. 小程序分享配置

```javascript
// 用户详情页 - onShareAppMessage
Page({
  onShareAppMessage() {
    const { user, inviteCode } = this.data;
    return {
      title: `${this.data.currentUser.name} 邀请您成为「${user.role === 'teacher' ? '老师' : '学生'}」`,
      path: `/pages/bind/bind?code=${inviteCode}`,
      imageUrl: '/images/invite-card.png'
    };
  }
});
```

## 流程总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户入驻流程                                   │
└─────────────────────────────────────────────────────────────────────────┘

                          ┌──────────────┐
                          │   新用户     │
                          └──────────────┘
                                 │
                    ┌────────────┴────────────┐
                    ▼                         ▼
           ┌──────────────┐          ┌──────────────┐
           │  收到邀请    │          │  自主注册    │
           │  小程序卡片  │          │  老师/学生   │
           └──────────────┘          └──────────────┘
                    │                         │
                    ▼                         ▼
           ┌──────────────┐          ┌──────────────┐
           │  绑定账号    │          │  独立用户    │
           │  设置密码    │          │  无从属关系  │
           └──────────────┘          └──────────────┘
                    │                         │
                    │                         │ 可选：接受邀请
                    ▼                         ▼
           ┌──────────────┐          ┌──────────────┐
           │  有从属关系  │◀─────────│  建立关系    │
           │  正常使用    │          │              │
           └──────────────┘          └──────────────┘
```

## 验收标准

- [x] 老师/学生可自主注册账号
- [x] 校长可添加老师和学生记录
- [x] 老师可添加学生记录
- [x] 用户列表显示正确的绑定状态
- [ ] 可发送邀请分享卡片到微信（需小程序认证）
- [x] 未注册用户可通过邀请完成绑定
- [ ] 已注册独立用户可接受邀请建立关系
- [x] 权限控制正确（老师不能看其他老师，只能看自己创建的学生）
- [x] 首页拆分为"我的老师"和"我的学生"两个入口

## 依赖

- 001-init-project.md（已完成）
- 002-user-auth.md（登录和绑定页面）

## 后续提案

- 004-schedule.md：排课功能




